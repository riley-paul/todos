import * as todos from "./todos.actions";
import * as users from "./users.actions";
import * as lists from "./lists.actions";
import * as listUsers from "./list-users.actions";

export const server = {
  todos,
  users,
  lists,
  listUsers,
};
