fragment Todo on TodoObjectType {
  id
  text
  isCompleted
  isAuthor
  author {
    ...User
  }
  list {
    id
    name
  }
}

query GetTodos($listId: ID) {
  todos(listId: $listId) {
    ...Todo
  }
}
